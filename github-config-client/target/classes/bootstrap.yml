#spring:
#  cloud:
#    config:
#      name: config-client
#      profile: dev
#      label: master
#      discovery:
#        enabled: true
#        service-id: github-config-server
#eureka:
#  client:
#    service-url:
#      defaultZone: config-client-dev.yml

spring:
  application:
    name: config-client
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:8083/
server:
  port: 8881