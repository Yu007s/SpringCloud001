server:
  port: 8762
spring:
  application:
    name: native-config-server
  profiles:
    active: native #表示读取本地的
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared #表示配置文件路径